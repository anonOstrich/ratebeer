<h1>Change password for <%= current_user.username if current_user %></h1>

<%= render 'form', user: @user %>

<%= link_to 'Show', @user, class: "btn btn-primary"   %> |
<%= link_to 'Back', users_path, class: "btn btn-primary"   %>
