<table class="table table-hover">
  <thead>
    <tr>
      <th></th>
      <th><strong>average</strong></th>
    </tr>
  </thead>

  <tbody>
    <% things.each do |thing| %>
      <tr>
        <td><%= link_to "#{thing.name}", thing %></td>
        <td><%= thing.average_rating %></td>
      </tr>
    <% end %>
  </tbody>
</table>